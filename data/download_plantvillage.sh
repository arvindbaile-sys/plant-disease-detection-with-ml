#!/bin/bash
# Download dataset manually here
#!/bin/bash
# Manual / kaggle download hint
# Option A: Use kaggle CLI (requires kaggle API token configured)
# Example (replace dataset slug if needed):
# kaggle datasets download -d emmarex/plantdisease -p data/raw --unzip


echo "Please download PlantVillage or a plant-disease dataset and unzip into data/raw/"